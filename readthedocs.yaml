version: 2

# Build documentation in the docs/ directory with MkDocs
mkdocs:
  configuration: mkdocs.yml

# Optionally set the version of Python and other tools you are using
python:
  version: 3.8
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
